# 📅 Day 05 Daily Log: Basic Aggregations & Dimensional Reduction

- **Date**: 2026-09-05
- **Subdomain**: Basic Aggregation
- **Challenges Completed**: 10 / 10
- **Concepts Mastered**:
  - `COUNT(*)` vs `COUNT(col)` physical row mechanics.
  - `SUM()`, `AVG()`, `MIN()`, `MAX()`.
  - `NULL` handling caveats (`AVG` invisible denominator bug, `COALESCE` on empty sums).
  - Mathematical rounding (`FLOOR` vs `CEIL` vs `ROUND`).
  - `WHERE` vs `HAVING` pipeline execution order.
  - Compound aggregation & frequency ranking (`Top Earners`).
- **Points Earned**: +130.00 Pts
- **Status**: Complete & Verified ✅
